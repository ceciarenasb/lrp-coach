---
description: Launch the LRP Coach app locally. Use when the user wants to start, run, or open the app.
disable-model-invocation: true
allowed-tools: Bash
---

Run the launch script and open the browser:

```bash
./scripts/launch.sh
```

If it reports "already running", the server is up — open http://localhost:7860 in the browser with `open http://localhost:7860`.

Report the URL and confirm the app is accessible.
